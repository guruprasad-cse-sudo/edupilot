# EduPilot v4.0 — Python dependencies (pinned to installed versions)
# Python 3.12 | Install with: pip install -r requirements.txt

# ── Streamlit UI ─────────────────────────────────────────────────────────────
streamlit==1.60.0
streamlit-authenticator==0.4.2
bcrypt==5.0.0
PyYAML>=6.0

# ── LLM & Orchestration ──────────────────────────────────────────────────────
langchain==0.2.5
langchain-groq==0.1.6
langchain-community==0.2.5
langchain-core==0.2.43
groq==0.9.0

# ── HTTP client — groq 0.9.x passes `proxies` to httpx; httpx 0.28+ removed
# that argument.  Pin to the last compatible release until groq is upgraded.
httpx>=0.27.0,<0.28.0

# ── RAG / Embeddings ─────────────────────────────────────────────────────────
# NOTE: rag.py uses FastEmbedEmbeddings (ONNX, no torch) — NOT
# sentence-transformers / langchain-huggingface. Those were removed here
# because they were unused dead weight (leftover from an earlier version)
# and `fastembed` — the package actually imported at runtime — was
# missing entirely, which is why embedding/retrieval was failing.
faiss-cpu==1.15.0
fastembed==0.3.6

# ── Document I/O ─────────────────────────────────────────────────────────────
python-docx==1.1.2
reportlab==4.2.2
pypdf==4.2.0
pdfplumber==0.11.9

# ── Configuration ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Utilities ────────────────────────────────────────────────────────────────
pydantic==2.7.4

# ── Charts ───────────────────────────────────────────────────────────────────
plotly>=5.22.0
